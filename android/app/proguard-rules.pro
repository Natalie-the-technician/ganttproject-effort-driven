# The release build is not minified today (see app/build.gradle.kts), so this
# file is a placeholder for when it is. The core module is plain Kotlin with
# no reflection, so it needs no keep rules of its own.
